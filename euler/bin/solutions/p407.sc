package solutions

object p407 {
	// a * a = a (mod n)
	// a := n - b
	// b * b = k * n - b
	// b * b + b : n, b -> min
	// b * (b + 1) : n
	// n is prime -> b = p - 1
	// n = p^k -> b = n - 1
	// n = p * q -> ???
}